---
title: "bazable accept"
sidebarTitle: "accept"
description: "Accept a pending AI-generated schema change proposal and apply it to your contract."
keywords: ["bazable accept", "accept proposal", "apply schema change", "AI contract update"]
---

Accepts a pending AI‑generated schema change proposal (from `bazable propose`) and applies it to your contract.

## Usage

```bash
bazable accept <proposal-id>
```

## Options

None.

## Example

```bash
bazable accept 42
```