---
title: "bazable ci"
sidebarTitle: "ci"
description: "Generate a GitHub Actions workflow that runs bazable inspect on every pull request."
keywords: ["bazable ci", "GitHub Actions", "CI integration", "contract enforcement CI"]
---

Generates a `.github/workflows/bazable.yml` file that runs `bazable inspect` on every pull request to `main`.

## Usage

```bash
bazable ci
```

## Options

None.

## Example

```bash
bazable ci
```