---
title: "bazable mcp"
sidebarTitle: "mcp"
description: "Start a Model Context Protocol server so AI coding agents can interact with Bazable natively."
keywords: ["bazable mcp", "Model Context Protocol", "MCP server", "AI agents", "AI integration"]
---

Starts a Model Context Protocol (MCP) server so that AI coding agents can interact with Bazable natively.

## Usage

```bash
bazable mcp
```

## Options

None.

## Example

```bash
bazable mcp
```